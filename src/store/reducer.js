import * as actionTypes from './actions';

const initialState = {
    ingredients: null,
    totalPrice: 3
}

const reducer = (state = initialState, actions) => {
    return state;
}

export default reducer;